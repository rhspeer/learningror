%h1 Listing orders

- if flash[:notice].present?
  %flash= flash[:notice]

%table.orders
  %tr
    %th Customer name
    %th Customer email
    %th Brand
    %th Frame
    %th Description
    %th Price
    %th Completed on
    %th Paid for on
    %th
    %th
    %th
    %th

  - @orders.each do |order|
    %tr
      %td= order.customer_name
      %td= order.customer_email
      %td= order.frame.brand.name
      %td= order.frame.name
      %td= order.description
      %td= order.price
      %td= order.completed_on.to_formatted_s(:short_date) unless order.completed_on.blank?
      %td= order.paid_for_on.to_formatted_s(:short_date) unless order.paid_for_on.blank?
      %td= link_to 'Show', order
      %td= link_to 'Edit', edit_order_path(order)
      %td= link_to 'Destroy', order, :method => :delete, :data => { :confirm => 'Are you sure?' }
      %td
        - if order.completed_on.blank?
          = link_to 'Mark Completed', mark_completed_order_path(order), method: :post
      %td
        - if order.paid_for_on.blank?
          = link_to 'Mark Paid', mark_paid_order_path(order), method: :post

%br

= link_to 'New Order', new_order_path
